{% extends "layout.njk" %}

{% block title %}
    <title>Foodfy - Receitas</title>
{% endblock %}

{% block header %}

    <header class="header-receitas">
        <div class="logo-receitas">
            <a href="/">
                <img src="/image/logo-receitas.png" alt="logo">
            </a>
        </div>
    </header>

{% endblock %}
        
{% block content %}
    <section class="receitas-mais-acessadas">
        <div class="container">

            <div class="page-index-topo">
                <h2>Gerenciar receitas</h2>
                <a href="/receitas/create" class="button">Nova receita</a>
            </div>

            <div class="receitas">
                {% for recipe in recipes %}
                    <div class="card" id="{{ recipe.id }}">
                        <div class="card-img" style="background-image: url({{ recipe.url_imagem }})"></div>
                        <p class="receita-title">{{ recipe.titulo }}</p>
                        <span class="receita-autor">{{ recipe.autor }}</span>
                    </div>
                {% endfor%}
            </div>

        </div>
    </section>
{% endblock %}